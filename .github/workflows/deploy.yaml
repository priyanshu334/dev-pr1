name: Deploy Task API

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6  # Upgraded to v6 for Node 24 support

      - name: Setup Go
        uses: actions/setup-go@v6  # Upgraded to v6 for Node 24 support
        with:
          go-version: "1.23"       # Fixed: Change this to your actual required Go version (e.g., "1.23" or "1.22")

      - name: Login to Docker Hub
        uses: docker/login-action@v4  # Upgraded to v4 for Node 24 support
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/go-pr:latest .

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/go-pr:latest

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

      - name: Deploy to EC2
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
            cd go-pr
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/go-pr:latest
            docker compose down
            docker compose up -d
          EOF
